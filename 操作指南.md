# 操作指南

## 写博客

在 `content/posts/` 下新建 `.md` 文件即可，无需修改代码。

格式示例 `content/posts/my-post.md`：

```markdown
---
title: "文章标题"
date: "2025-04-01"
excerpt: "文章摘要，会显示在列表页和首页卡片上"
category: "分类名"
---

正文内容，支持 Markdown 语法：

## 二级标题

**粗体** *斜体* `行内代码`

- 列表项
- 列表项

> 引用

[链接文字](https://example.com)

```javascript
代码块
```
```

- `title`、`date`、`excerpt`、`category` 是必填的 YAML 头部字段
- `slug` 由文件名决定（`my-post.md` → `/blog/my-post`）
- 文章按日期降序排列（最新的在最前）
- 首页自动取最新 3 篇

## 修改导航栏

编辑 `config/site.json` 中的 `navigation` 数组：

```json
{
  "navigation": [
    { "name": "Home", "href": "/" },
    { "name": "Blog", "href": "/blog" },
    { "name": "About", "href": "/about" },
    { "name": "Projects", "href": "/projects" }
  ]
}
```

## 修改社交链接 / 站点信息

编辑 `config/site.json`：

```json
{
  "name": "Tasrovy",
  "description": "Personal developer portfolio and blog",
  "email": "hello@tasrovy.com",
  "location": "Remote",
  "social": {
    "github": "https://github.com/xxx",
    "linkedin": "https://linkedin.com/in/xxx",
    "twitter": "https://twitter.com/xxx"
  }
}
```

- `name` 会显示在导航栏 Logo 和页脚版权信息
- `social` 下的链接会显示在 `/about` 页面

## 修改项目列表

编辑 `config/projects.json`：

```json
[
  {
    "title": "项目名称",
    "description": "项目描述",
    "technologies": ["React", "TypeScript"],
    "link": "https://...",
    "github": "https://github.com/..."
  }
]
```

## 常用命令

| 命令 | 用途 |
|------|------|
| `npm run dev` | 启动开发服务器 (localhost:3000) |
| `npm run build` | 生产构建 |
| `npm run lint` | 代码检查 |
| `npm run deploy` | 构建并部署到 Cloudflare |
| `npm run preview` | 构建并在本地预览 Cloudflare 版本 |
